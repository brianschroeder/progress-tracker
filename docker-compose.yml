version: '3.8'

services:
  goal-tracker:
    build: .
    container_name: goal-tracker
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DB_PATH=/app/data/goals.db
    restart: unless-stopped
